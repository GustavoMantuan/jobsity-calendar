import styled from 'styled-components'

export const DailyRemindersWrapper = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-content: center;
  justify-content: space-around;
  margin-top: 0.1rem;
  position: relative;
`
export const CleanButton = styled.div`
  cursor: pointer;
  position: absolute;
  right: 10px;
  top: -20px;
  z-index: 0;
`
